%packages
%include /mnt/install/repo/ks/ks_ids_server.cfg.packages
foss-ids
%end

%post
#!/bin/sh
set -xv
exec 1>/root/kickstart-stage2.log 2>&1
# We also run ansible if the inventory or playbooks change
cd /opt/ansible
ansible-playbook -i inventory ./playbooks/ids.yml
%end
